apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  annotations:
    flux.weave.works/tag.helloworld: glob:master-*

      vad

      vad
    flux.weave.works/locked_user: simon <simon@weave.works>
    flux.weave.works/tag.helloworld: glob:master-*

      do

      You

      Mean
    flux.weave.works/locked_user: simon <simon@weave.works>
    flux.weave.works/tag.helloworld: glob:master-*
  name: helloworld
spec:
  minReadySeconds: 5
  replicas: 2
  template:
    metadata:
      labels:
        name: helloworld
    spec:
      containers:
      - name: helloworld
        image: quay.io/weaveworks/helloworld:master-07a1b6b
        args:
        - -msg=Ahoy
        ports:
        - containerPort: 80
      - name: sidecar
        image: quay.io/weaveworks/sidecar:master-a000002
        args:
        - -addr=:8080
        ports:
        - containerPort: 8080
